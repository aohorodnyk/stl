// This package is initially was copied from golang.org/x/exp/constraints.
// It's useful since this package provides a list of supported types for generics feature.
//
// This package was copied, instead of setting it as dependency, because golang.org/x/exp does not use versioning
// so, we cannot guarantee the dependency version and the stability of it.
package constraints
